{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "chmod +x ./railway/init-app.sh && sh ./railway/init-app.sh && php artisan migrate:status && php artisan migrate --force && php artisan db:seed --force && php artisan storage:link || true && php artisan config:cache && php artisan route:cache && php artisan view:cache && apache2-foreground",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

